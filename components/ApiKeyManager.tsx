import React from 'react';

const ApiKeyManager: React.FC = () => {
    // Per coding guidelines, API key must be managed via environment variables.
    // This UI component is therefore disabled.
    return null;
};

export default ApiKeyManager;
