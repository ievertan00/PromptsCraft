import { ParameterArray, ParameterMap, SQLReturnType, ReturnMap, Pointer } from './sqlite3-types';
import Database from './Database';
export default class Statement {
    private readonly wasm;
    private stmt;
    private readonly db;
    private pos;
    private readonly allocatedmem;
    constructor(stmt: Pointer, db: Database);
    bind(values: ParameterArray | ParameterMap): void;
    private bindValue;
    step(): boolean;
    get(params?: ParameterArray | ParameterMap): SQLReturnType[];
    getColumnNames(): string[];
    getAsObject(params?: ParameterArray | ParameterMap): ReturnMap;
    run(values?: ParameterArray | ParameterMap): boolean;
    reset(): boolean;
    private freemem;
    free(): boolean;
}
//# sourceMappingURL=Statement.d.ts.map